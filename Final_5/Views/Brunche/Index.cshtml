@model IEnumerable<Final_5.Models.Brunch>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.practicsName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.city)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.street)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.practicsName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.city)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.street)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.brunchNameId }) |
            @Html.ActionLink("Details", "Details", new { id=item.brunchNameId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.brunchNameId })
        </td>
    </tr>
}

</table>
